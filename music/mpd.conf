# MPD Configuration
# Location of mpd files
music_directory     "~/Music"
playlist_directory  "~/.local/share/mpd/playlists"
db_file             "~/.local/share/mpd/database"
log_file            "~/.local/share/mpd/log"
pid_file            "~/.local/share/mpd/pid"
state_file          "~/.local/share/mpd/state"
sticker_database    "~/.local/share/mpd/sticker.db"

# Network
bind_to_address     "localhost"
port                "6600"

# Audio output - PipeWire native
audio_output {
    type            "pipewire"
    name            "PipeWire Output"
}

# Fallback if Pipewire doesn't work: uncomment this and comment out the block above:
# audio_output {
#     type            "pulse"
#     name            "PulseAudio (PipeWire compat)"
# }

# Resampling (optional, good for quality)
samplerate_converter    "medium"

# Log level: default, secure, verbose
log_level               "default"
